# docker-compose.yml — postgres для локального запуска
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: tamagochi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
